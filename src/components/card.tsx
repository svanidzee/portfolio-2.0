'use client';

import { Center, Text, Box, Heading, Grid } from '@chakra-ui/react';
import { Works } from '../../generated/schema';

interface IndexProps {
  projects: Works[];
}

const Card = ({ projects }: IndexProps) => {
  return <div>1</div>;
};

export default Card;
